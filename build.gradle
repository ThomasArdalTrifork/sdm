allprojects {
	group: 'com.trifork'
	version: '3.0-SNAPSHOT'
}

subprojects {
	apply plugin: 'java'
	apply plugin: 'eclipse'

	repositories {
		mavenCentral()
	}

	dependencies {
		compileTest group: 'junit', name: 'junit', version: '4.8.2'
	}
}
